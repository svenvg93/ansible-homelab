- name: Set timezone using timedatectl
  ansible.builtin.command: "timedatectl set-timezone {{ bootstrap_timezone }}"
  changed_when: true

- name: Ensure locale is set
  ansible.builtin.command: "localectl set-locale LANG={{ bootstrap_locale }}"
  changed_when: true
